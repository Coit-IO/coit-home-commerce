apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "coit-hc-ad-svc-release.fullname" . }}-test-connection"
  labels:
    {{- include "coit-hc-ad-svc-release.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "coit-hc-ad-svc-release.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
